def labels = ['master', 'master'] // labels for Jenkins node types we will build on
def builders = [:]
for (x in labels) {
    def label = x 
    builders[label] = {
      node(label) {
	echo "EXECUTING TESTS IN PARALLEL ON ALL AGENTS"
	sleep 10
      }
    }
}

parallel builders
